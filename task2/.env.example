# Server-side only — never NEXT_PUBLIC_ (token stays in route handlers, not the browser)
V1_API_BASE_URL=https://api.fairpl.ai
FAIRPLAI_TOKEN=your-jwt-token-here

# When V1_API_BASE_URL is unset the app uses fixture data automatically (demo mode)
